#include<iostream>
#include<conio.h>
int main()
  {
    clrscr();
    int no;
    cout<<"Enter any number\n";
    cin>>no;
    if(no>0)
    cout<<"no is positive";
    if(no<0)
    cout<<"no is negative";
    if(no==0)
    cout<<"no is zero";
    getch();    
}
